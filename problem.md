用户希望我将他整理的面试问题和答案整理成markdown格式，方便他复制粘贴。这是一个文档整理任务，我需要将用户提供的内容按照清晰的markdown格式组织好。

让我分析一下内容结构：
1. Agent问题
2. 文献写作耗费token详情
3. 为什么从算法改为agent评测？
4. 基模评测和agent评测的区别
5. 如何理解agent
6. 如何评估agent效果
7. 人评和机评配合
8. Critic检测
9. Reader解析准确率
10. Agent产品评估体系设计
11. 商业化落地差距
12. 效率问题解决
13. 提示词模板设计
14. 判断prompt变好
15. 单agent还是多agent
16. 其他架构尝试
17. Agent任务拆分
18. 拆分粒度问题
19. 上下文构建
20. 避免上下文污染
21. 上下文窗口不够
22. 上下文压缩
23. 模型幻觉原因
24. 降低幻觉方法
25. Rag实现
26. 作业帮实习相关（Rag知识点、流程、数据集、意图区分等）

我需要将这些内容整理成清晰的markdown格式，使用合适的标题层级、列表、代码块等元素。
我将为你整理成规范的Markdown格式：

```markdown
# Agent评测岗位面试问题整理

## 一、Agent基础概念

### 1. 什么是Agent？

**Agent = LLM + Prompt + Tool 组成的智能体，能自主完成特定任务。**

- **Agent** 像"主管"，负责理解意图、规划任务、调用工具
- **Skill** 像"助理"，专门执行具体操作

**常见框架：**
- LangChain Agent Chain：意图识别直接调用 Skill
- ReAct：思考-行动-观察循环

**我设计的Agent架构：**
- 多Agent协作，Plan-and-Execute模式
- 流程：先规划任务分解 → 逐步执行各子任务 → 汇总结果

**文献综述生成Agent示例：**
- Planner：规划检索策略
- Searcher：检索论文
- Reader：解析内容
- Writer：生成综述
- Critic：校验引用

---

### 2. 如何理解Agent？与普通大模型问答的最大区别是什么？

**Agent = LLM + 工具调用 + 自主决策 + 任务执行**

| 维度 | 普通大模型 | Agent |
|------|-----------|-------|
| **能力** | 只能回答问题 | 可以执行任务、调用工具 |
| **信息来源** | 用户输入文本 + 自身知识 | 项目文件、数据库、API、搜索 |
| **决策方式** | 被动回答问题 | 自主决定下一步操作 |
| **交互模式** | 单次问答 | 多步骤任务（ReAct范式、Loop） |
| **操作能力** | 仅文本生成 | 对文本、文件、代码执行操作 |

---

### 3. 单Agent还是多Agent？为什么这么设计？

**我选择多Agent架构，原因如下：**

**任务复杂度高：**
- 文献综述撰写需要：意图理解 → 检索规划 → 论文搜索 → PDF下载 → 文档解析 → 综述生成 → 引用审查
- 单Agent的Prompt需要描述所有任务，过长容易导致LLM遗漏或输出混乱

**串行流程，任务可精准拆分：**
- 流程中可能出现：arXiv API超时、PDF下载失败、LLM响应慢
- 需要设置断点保存，某个环节失败时能干净地取出之前的结果
- 多Agent架构能精准定位问题所在步骤

**优势：**
- 快速定位和解决问题
- 扩展/缩减功能、修改Prompt更清晰简单
- 每个Agent专注一个任务，失败可单独重试
- 可独立优化和扩展

---

### 4. 有没有尝试过其他架构？

**我分析过多种架构：**

| 架构类型 | 分析结果 |
|---------|---------|
| **单Agent** | Prompt过长，LLM容易遗漏或混乱，不适合 |
| **ReAct** | 文献综述撰写是明确的串行任务，不需要LLM自主决策，不适合 |
| **多Agent（Plan-and-Execute）** | 每个Agent专注一个任务，出错时精准定位，可独立优化扩展，**最终选择** |

---

### 5. Agent的任务是怎么拆分的？拆分粒度怎么确定？

**拆分原则：**

**原则1：按照流程拆分**
- 文献综述撰写流程：检索规划 → 论文搜索 → PDF下载 → 文档解析 → 写综述 → 引用审查
- 每个流程步骤对应一个Agent

**原则2：每个Agent只负责一个核心任务**
- 输入输出清晰，可独立测试
- 示例：Searcher合并了搜索与下载（紧密相关）

**拆分粒度确定：**
- 相关任务可以合并，独立任务应该拆分
- 考虑Prompt的长度范围

**拆太细和拆太粗的问题：**

| 问题类型 | 具体问题 |
|---------|---------|
| **拆太细** | Agent数量过多，编排复杂；中间状态传递频繁；Prompt太短，信息不足，LLM理解不完整；不必要的步骤导致用户体验差 |
| **拆太粗** | Prompt过长，LLM容易遗漏或混乱；出错时难以定位问题；无法针对性优化某个任务 |

---

## 二、Agent评测相关

### 1. 为什么从算法改为Agent评测？

**首先，Agent是当前AI领域最具挑战性、也最有意思的方向之一。**

我做算法时一直在思考：模型在benchmark上刷到很高分数，但它是否真的能成为一个好用的Agent？这就是评测需要回答的问题。

**其次，做评测并不比做算法简单。**

要做好Agent评测，需要：
- 深入理解Agent的架构设计
- 设计科学合理的评估体系
- 定义什么叫"好"的标准

这些都是技术活，需要有算法背景才能做好。

**我的优势：**
- 对模型能力的理解
- 对指标体系的认识
- 对实验设计的经验
- 设计对比实验、分析模型薄弱环节、从大量数据中找出规律的能力
- 对模型"不合理行为"有更敏锐的直觉

**未来规划：**
- 尽快熟悉现有的评测框架
- 分享Multi-Agent项目实践经验
- 长期参与构建更完善的Agent评估体系

---

### 2. 基模评测和Agent评测的区别

| 维度 | 基模评测 | Agent评测 |
|------|---------|----------|
| **评测目标** | 模型能力边界（翻译、代码生成等单点能力） | 端到端的任务完成度 |
| **评测指标** | 标准化指标（MMLU、GSM8K等准确率类指标） | 多维指标体系：任务成功率、工具调用准确率、规划合理性、效率（Token/步数/延迟）、鲁棒性（自我修复、意图澄清）、安全性 |
| **评测环境** | 基于静态数据集 | 动态交互环境（沙箱、浏览器等），环境状态随操作变化，可复现性更差 |
| **评测方法** | 主要看结果对不对 | 结果+过程双重评估，需看轨迹是否合理，可能需要LLM-as-Judge、规则检查等复合评分方式 |
| **评测难点** | - | 环境搭建成本高、任务完成标准难定义（部分成功怎么算）、长链路错误传播（一步错步步错） |

---

### 3. 如何评估Agent效果？如何构建评测集？

**以Planner为例：**

**评测目标：** 输入模糊意图 → LLM进行query改写 → 按改写的英文query进行arXiv搜索

**评测集构建：**
- 建立GT（Ground Truth）：设置多条预期生成的检索式
- arXiv检索式需要特定格式，格式也可作为评测项

**评测指标：**
- 关键词覆盖率：计算生成的检索式是否包含预期关键词
- 检验检索式是否能被arXiv正确解析

**以Searcher为例：**

**任务：** 根据检索query找寻并筛选论文，下载要求论文数量的两倍，按Semantic Scholar引用量排序

**评测集构建：**
- 按照生成的query去arXiv检索
- 按检索要求设置标准，下载相关论文作为GT

**评测指标：**
- Recall@K、Precision@K
- 排序质量：NDCG或MRR

**注意：** 引用量高的相关性并不一定强，需修改排序策略（混合排序：引用量+相关性），使用相关性得分需建立相关性等级。

**以Reader为例：**
- 检查提取的标题、作者信息、核心内容、图片图表是否正确

**以Writer为例：**
- 需要人工评估

---

### 4. 人评和机评怎么配合？

**目前：** 全自动纯机评流程，机评为主

**引入人评的设计：**

**Reader解析阶段：**
- 机器解析后，人工抽检10%结果
- 检查结构化信息（标题、作者、核心方法）是否有误
- 抽检错误率超过阈值（如5%），触发全量复核

**Writer生成阶段：**
- Critic做形式校验（引用是否捏造）
- 内容质量：LLM自检 + 人工审核
- 对生成综述的每个章节抽查一段，检查是否符合原文事实
- 主观分析Writer写的内容质量

---

### 5. Critic检测出引用无效的比例是多少？

**测试结果：** 多次测试都是通过白名单校验的，暂未检测出无效引用。

---

### 6. Reader解析的准确率怎么评估？

**目前验证方式：**

**方式1：LLM自检**
- 从原文随机抽一段话
- 与Reader提取的结构化信息对比
- 检查是否一致

**方式2：后道工序验证**
- Writer生成内容出现明显事实错误
- 通过Critic人工复核发现
- 倒推Reader提取是否有问题

**严格评估设计：**
- 人工标注一批论文的Ground Truth结构化信息
- 跑Reader看提取的准确率、召回率

---

### 7. 如果让你给这个Agent产品设计评估体系，你会怎么设计？

**检索阶段：**
- 召回率衡量Searcher效果
- 从下载论文中随机抽10篇，人工判断相关性

**解析阶段：**
- Reader结构化信息的准确率
- 抽检标题、作者、核心方法是否提取正确

**生成阶段：**
- Critic的拦截率衡量幻觉程度
- 人工抽检内容是否与原文一致

**最终效果：**
- 用户满意度评分
- 生成综述字数是否符合预期
- 参考文献格式是否正确

---

### 8. Agent离商业化落地还差什么？

| 差距项 | 具体内容 |
|-------|---------|
| **稳定性** | 当前单Agent串行执行，部分模块失败影响全流程，需加监控告警和降级策略 |
| **评估体系** | 量化指标未完全建立，需定义清晰的效果指标 |
| **性能优化** | Reader串行是瓶颈，需改成并行或分层解析 |
| **产品化** | 需做用户界面、API接口、权限管理 |
| **成本控制** | 需更精细的token消耗追踪和优化 |

---

## 三、Agent技术细节

### 1. 文献写作耗费Token详情

**测试数据（30篇论文）：**
- Reader解析时间：3-4小时
- Reader调用次数：每篇论文调用一次
- Writer之前Token：5万多
- 模型：DeepSeek V4 Flash
- 花费：约0.03元
- Writer Token：总Token的一半不到，4万多
- 论文篇幅：10-35页（20多页居多）
- 最终综述：约28页
- 总用时：4个多小时

**规律：**
- 输入Prompt tokens远多于输出Completion tokens
- 原因：每次LLM调用都需把论文结构化信息和写作要求作为上下文传入

---

### 2. 30篇论文3-4小时，效率问题如何解决？

**主要从Reader入手：**

**方案1：分层解析**
- 先用轻量级模型只提取文字部分，速度快
- 图表部分按需解析

**方案2：并行化**
- Reader每篇论文解析互相独立
- 改成线程池并发
- 30篇如果5篇并发，时间可从3-4小时缩短到1小时左右

**方案3：增量解析**
- 向量库已有历史论文直接跳过
- 不需要重新下载和解析

---

### 3. 提示词模板如何设计与迭代？

**以Planner（query改写）为例：**

**提示词设计：**
- 角色设定：学术检索专家
- 任务描述：生成4角度检索式（精确检索式 + 同义近义 + 相关技术方向 + 综述背景）
- 格式约束：JSON输出
- 示例：正确/错误对比

**迭代过程：**
- 初始版本：不包括四角度检索和示例，核心概念未使用OR扩展同义词
- 发现问题：query改写不够精准，涉及区域太窄
- 改进：分析问题原因，针对性调整Prompt

---

### 4. 如何判断一个Prompt模板是真的变好了？

**多维度评估：**

**1. 输出质量**
- 格式是否正确
- 内容是否准确
- 任务是否完成
- 需建立测试集计算召回等指标

**2. 稳定性**
- 相同输入多次测试
- 判断输出是否稳定
- 异常处理是否正确

**3. 效率**
- Prompt长度是否合理
- 响应时间如何
- Token消耗如何

**对比实验：**
- 输入模糊查询
- 对比Prompt修改前后query改写的检索式
- 判断是否符合要求（主观评估）

---

### 5. 上下文是如何构建的？

**LLM调用时传入的完整Prompt内容，一般包括：**
- 角色设定
- 任务描述
- 输入数据
- 约束条件
- 示例

**以Planner为例：**
- 角色设定：学术文献检索专家
- 任务描述：生成4角度arXiv检索式
- 用户主题
- 格式约束：输出严格JSON格式，包含要求的字段
- 示例：用户输入后正确的输出和错误的输出
- 绝对规则约束

---

### 6. 如何避免上下文污染或信息冲突？

**以Writer为例：**

**流程：** Reader输出JSON → Writer加载 → 构建上下文 → LLM生成LaTeX

**避免方法：**

**1. 结构化分隔**
- 使用明确分隔符区分每篇论文信息
- 每篇论文信息独立呈现
- 论文1信息不会被当作论文2的

**2. 白名单约束和引用校验**
- 只允许引用已下载论文
- LLM引用不在白名单的论文会被拦截并标记
- 正则匹配cite是否在白名单中
- 防止LLM虚构引用

**3. 论文分类**
- 按技术分类（如MoE分为专家路由类和负载均衡类）
- 专家路由章节只传论文1和2，不传3
- 减少信息冲突，LLM只看到相关论文

**4. Prompt明确规则**
- 只能引用提供的文献ID
- 严禁捏造
- 论文结论冲突时要求LLM分别陈述
- 禁止捏造结论

---

### 7. 如果上下文窗口不够，优先保留哪些信息？

**优先级排序：**

**必须保留：**
- 任务描述
- 核心数据（论文标题+核心方法）
- 约束条件（引用规则）

**可以精简：**
- 作者和年份等信息
- 详细实验描述

**可以直接删除：**
- 示例（通过Prompt约束规则弥补）

**已实施的措施：**
- 分块生成
- 论文按技术路线分类
- 每个章节只传相关论文，不传所有论文

---

### 8. 有没有做过压缩？

**目前未做过压缩**，调试过程中一直在增加必要的Prompt。

**了解的压缩方法：**

**1. Prompt层面压缩**
- 删除示例
- 删掉作者信息、论文年份等

**2. LLM压缩**
- Reader输出JSON后，让LLM精简信息

**3. 源头压缩**
- 检索下载时直接摒弃不符合要求的论文
- 不构成JSON输入到Writer提示词中

---

### 9. 模型产生幻觉的常见原因

**1. 训练数据不足或偏差**
- 模型对某些领域知识不足或学到错误信息
- 问小众领域知识细节时可能虚构

**2. 提示词约束不够**
- 需在Prompt中加入零幻觉规则、绝对规则、极其重要等约束

**3. 上下文信息不足**
- 上下文缺少关键信息，LLM可能补充虚构信息
- LLM输出超出上下文信息

**4. 提示词太长**
- 可能导致LLM注意力分散

---

### 10. 工程上有哪些办法可以降低幻觉？

**1. RAG检索增强**
- 提供真实信息，约束生成范围
- Reader输出JSON → Writer加载并构建上下文

**2. Prompt约束**
- 加入绝对规则、0幻觉规则、极其重要等关键词
- 让大模型注意规则

**3. 白名单约束**
- 只允许引用已下载论文
- 引用校验：Writer使用正则匹配cite，检查是否在白名单中

**4. Critic Agent进一步检查**
- 检查引用是否在白名单中

**5. 结构化分隔**
- Writer加载JSON时明确分隔符处理不同论文
- 避免上下文污染

**6. 论文分类**
- 按技术路线分类
- 每次只处理相关信息
- 减少信息冲突

---

### 11. RAG在这个过程中是怎么做的？

**RAG核心思想：**
- **检索**：从知识库中检索相关信息
- **增强**：将检索到的信息作为上下文输入LLM
- **生成**：LLM基于检索到的信息生成答案

**在文献综述Agent中的应用：**
- Writer从Reader输出的JSON中加载信息
- 作为Prompt的一部分构成提示词
- 输出LaTeX文件
- 基于真实信息生成

---

## 四、作业帮实习项目

### 1. RAG知识点

**向量维度：**
- simBERT输出：768维
- BGE-M3输出：1024维
- 向量库维度取决于编码模型，配置时dims参数要和编码模型输出维度一致

**FAISS和Elasticsearch都支持任意维度**

---

### 2. 智能客服与模块化RAG系统设计流程

**离线链路：**
- 文档处理（使用MinerU库）
  - 表格识别为markdown格式
  - 图片处理（后期可加敏感图片过滤）
- 文本清洗与分块（overlap=50）
- BGE-M3向量化（1024维）
- 存入Elasticsearch
- 混合索引：全文索引 + 向量索引

**在线链路：**
1. 用户提问
2. 敏感词过滤 + 同义词替换预处理
3. 意图识别（BERT-small微调）
   - 简单意图（置信度≥0.85）：
     - 查Redis缓存 → 有则返回
     - 无则FAQ检索（simBERT-4L匹配）→ 返回预设答案
   - 复杂意图（置信度<0.85）：
     - Qwen-3进一步判断
     - BGE-M3编码问题 → ES混合检索
     - Reranker二次重排 → 选top-3
     - 拼装Prompt（角色设定+约束规则+检索信息）
     - Qwen-3 7B生成答案

---

### 3. Elasticsearch混合检索

**向量检索：**
- BGE-M3编码问题为1024维向量
- ES的dense_vector字段做KNN检索
- 余弦相似度计算

**全文检索：**
- 关键词匹配
- 标准text字段查询

**合并结果：**
- 两路结果合并取top-10
- Reranker重排选top-3
- 作为构建Prompt基础

---

### 4. 数据集如何构建？提示词怎么写？大概多少条？

**数据来源：**
- 历史客服日志
- 业务知识库抽取的QA对

**数据增强：**
- 使用DeepSeek和Qwen语言大模型
- 对同一问题生成不同回答
- 生成负样本

**提示词：**
- 角色设定：教育客服售卖课程专家
- 任务和约束条件
- 输出格式

**数据量：** 约5000多条微调数据

---

### 5. 简单意图和复杂意图怎么区分？阈值是多少？

**判断逻辑：**
- BERT-small或Qwen-3微调做意图分类
- 输出向量（维度=意图类别数）
- 经过softmax计算，输出概率
- 取最大概率确定意图

**阈值设定：**
- ≥0.85：简单意图
- <0.85：复杂意图

---

### 6. Qwen-3 7B和Qwen-Turbo什么时候用哪个？

**最终选择：Qwen-3 7B**

**原因：**
- 复杂意图识别需要更好的理解能力
- 7B模型效果更稳定
- 使用vLLM加速推理（借鉴操作系统虚拟内存和分页思想，分页注意力机制）
- 速度能满足业务需求

**Qwen-Turbo适用场景：**
- 简单任务
- 成本敏感场景

---

### 7. 意图识别准确率是多少？怎么评估？

**准确率：** 92%-95%

**评估方法：**
- 从线上日志分层抽样构建1000条测试集
- 覆盖所有意图类别（退货、退款、改课等）
- 包含简单意图、复杂意图、模糊问题、越狱攻击等
- 人工标注每条问题的真实意图类别
- 导出JSON文件
- 模型预测结果与真实标签对比
- 计算Accuracy、Precision、Recall、F1-Score

**分层抽样目的：**
- 保证各类意图都有覆盖
- 避免某类意图样本太少导致评估偏差

---

### 8. 人工标注很耗人力，自动化方式怎么解决？

**LLM-as-Judge方案：**

**具体方法：**
- 将人工标注的说明文档转化为结构化Prompt
- 用更强的模型（GPT-4或Qwen-Max）对测试集进行意图标注
- LLM可理解复杂的意图定义和边界条件
- 标注质量接近人工

**进一步验证：**
- 人工复核
- 用更强的模型作为Judge评判大模型标注结果

---

### 9. 模型有犯错概率，如何规避幻觉问题导致的标注错误？

**重点：不只防止模型犯错，而是处理可能的幻觉结果**

**交叉验证方案：**
- 使用两个独立模型进行标注
- 将数据输入两个模型，输出标注结果
- 标注结果相同：幻觉概率极低，直接采用
- 标注结果不一致：人工复核

---

### 10. BGE-M3和simBERT-4L什么时候用哪个？

| 模型 | 维度 | 使用场景 |
|------|------|---------|
| **simBERT-4L** | 768维 | 在线FAQ问答匹配：simBERT-4L → FAQ库 → 直接返回答案 |
| **BGE-M3** | 1024维 | 构建知识库 + 复杂意图向量检索 |

**维度含义：** 把文本编码成向量后，这个向量有多少个数字。维度越高表示能力越强，但计算量也更大。

---

### 11. Elasticsearch向量检索的索引怎么建的？参数如何配置？阈值如何设置？

**向量检索：**
- 问题用BGE-M3编码成1024维向量
- Elasticsearch的dense_vector字段做KNN检索
- 余弦相似度计算

**全文检索：**
- 关键词匹配
- 标准text字段查询

**结果合并：**
- 两路结果合并取top-10
- Reranker重排选top-3
- 作为构建Prompt基础

**相似度阈值：0.75**
- 通过离线实验，绘制P-R曲线
- 找到准确率和召回率平衡的拐点
- 低于0.75的结果被过滤，防止引入噪声导致大模型幻觉

---

### 12. Redis缓存

**核心思想：** 将常用数据存在快速访问的地方

**流程：**
- 用户请求 → 判断意图类型
- 简单意图：查Redis缓存 → 有数据直接返回，无数据查FAQ
- 复杂意图：直接查数据库

**注意：** 复杂意图问答不存入Redis缓存

**热点回答定义：**
- 24小时内查询次数Top 1000的Query
- 缓存用户最常问的FAQ答案和用户问题匹配的FAQ答案
- 常问问题（如"购买之后去哪里看？""课程都包括什么"）不设置过期时间，后台更新

---

### 13. Redis常见数据结构及适用场景

| 数据结构 | 特点 | 适用场景 |
|---------|------|---------|
| **String** | 最基础 | 缓存、计数器、分布式锁 |
| **Hash** | 存储对象，修改字段无需序列化整个对象 | 用户信息、商品详情 |
| **List** | 双向链表 | 消息队列、时间线（Timeline） |
| **Set** | 无序去重 | 标签交集/并集（如共同好友） |
| **ZSet** | 带分数排序 | 排行榜、延迟队列 |

**在意图识别中的应用：**
- **String**：缓存热点FAQ完整回答（文本+向量ID），命中缓存直接返回，响应时间降低40%
- **Hash**：存储FAQ条目元数据（更新时间、询问次数、业务模块），更新时只需更新特定字段

---

### 14. Redis过期删除策略和内存淘汰机制

**过期删除策略：惰性删除 + 定期删除**

**惰性删除：**
- 用户访问key时检查是否过期
- 过期则删除

**定期删除：**
- 定期随机检查一批key
- 发现过期则删除
- 默认每秒10次，每次检查20个key

**内存淘汰机制：**
- 随机删除任意key
- 删除最近最少使用的key（LRU）
- 删除最不经常使用的key（LFU）
- 在快到过期时间的key中删除最近最少使用的
- 在过期时间key中随机删除

---

### 15. 缓存命中率是多少？怎么算的？

**命中率：35%-45%**

教育客服场景重复问题较多，命中率相对较高。

**计算方法：**
```bash
Redis INFO stats命令
keyspace_hits（命中次数）
keyspace_misses（未命中次数）

命中率 = keyspace_hits / (keyspace_hits + keyspace_misses)
```

---

### 16. 缓存过期时间为什么选择24小时？

业务场景特点：
- 用户问题有周期性
- 24小时能覆盖大部分重复查询
- 平衡缓存效率和内存占用

---

### 17. 什么是缓存穿透、缓存击穿、缓存雪崩？

**缓存穿透：**
- 用户询问的问题在FAQ和ES中都没有
- **解决方法：** 缓存空值，相同无效问题5分钟内返回空值

**缓存击穿：**
- Redis中key过期，大量请求同时访问
- **解决方法：** 热点key不设置过期时间（永不过期），后台定时任务每小时统计Top 1000，更新热点缓存

**缓存雪崩：**
- 大量key同时过期
- **解决方法：** 设置不同过期时间，避免同时过期

---

### 18. FAQ是什么？匹配准确率提升15%是怎么算的？

**FAQ：** 常见问答库，包含预设的问题-答案对

**准确率提升计算：**
- 从线上日志分层抽样一批问题
- 人工标注正确答案
- 对比优化前后的模型预测结果
- 优化前：约78%
- 优化后：约92%
- 提升：约15%

**分层抽样：**
- 按不同类别分别抽取样本
- 按比例从每个类别（退款、换班、课程咨询等）抽取
- 保证各类都有覆盖
- 评估更准确，能发现某些类别效果差的问题

---

### 19. PDF解析遇到表格、图片怎么处理？文本分块的overlap是多少？

**PDF处理：**
- 使用MinerU库
- 表格识别为markdown格式，转为文本形式
- 图片处理（后期可加敏感图片过滤）

**文本分块：**
- overlap = 50

---

### 20. 异步任务队列用的什么框架？

**asyncio**

---

### 21. 敏感词过滤的词库如何维护？如何加入新词？

**词库维护：**
- 沿用项目已有词库
- 定期更新，根据线上反馈和业务需求补充新词

**新词加入：**
- 计算新词与现有词库的相似度
- 相似度>0.7：可能是变体词或同义词，需人工审核后决定是否加入
- 相似度低：新类型的敏感词，直接加入词库

---

### 22. 检索使用的是什么检索？

**混合检索：向量检索 + 全文检索**

**向量检索：**
- BGE-M3编码问题为向量
- ES里做KNN搜索
- 找语义相似的文档

**全文检索：**
- 关键词匹配
- 找包含关键词的文档

**结果合并：**
- 两路结果合并取top-10
- Reranker重排选top-3

---

### 23. 数据库是怎么制作的？切分策略如何选择？向量模型用的啥？

**数据来源：**
- 项目已有知识库
- 业务相关文档（课程说明、退款政策等）

**处理流程：**
- MinerU解析
- 切分和向量化

**切分策略：**
- 按段落切分
- 每块512字符
- overlap 50字符
- 保证信息完整性

**向量模型：**
- FAQ检索：simBERT-4L（768维）
- 知识库检索：BGE-M3（1024维）

---

### 24. 文本数据多的时候，用户问题如何匹配？

**简单意图流程：**
- BERT-small判断置信度高
- simBERT-4L做FAQ匹配
- 直接返回预设答案
- 不需要LLM生成

**复杂意图流程：**
- BERT-small置信度低
- Qwen-3进一步判断
- BGE-M3检索知识库
- 拼装Prompt后Qwen-3生成

**评价检索效果：**
- 人工标注测试集
- 计算准确率

---

### 25. 匹配好结果后是塞进Prompt吗？

是的。

拼装Prompt：
- 问题和检索到的文档组合
- 形成完整的Prompt
- 约束LLM
- 让LLM知道基于什么来回答问题

---

### 26. 如何评价是否能找到正确的内容？

**评估方法：**
- 人工标注测试集
- 计算检索准确率、召回率
- 分析badcase
- 迭代优化检索策略
```