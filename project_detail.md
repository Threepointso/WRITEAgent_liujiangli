
c:\Users\LOVE\Desktop\write_agent\project_description.md
# 多 Agent 学术文献综述自动生成系统 — 项目详细描述

> **项目定位**：端到端的多 Agent 协作系统，输入模糊中文研究主题，自动完成文献检索、PDF 深度解析、结构化信息提取、LaTeX 综述撰写、引用零幻觉校验的全流程。

---

## 一、项目背景与动机

### 1.1 痛点分析

在学术研究中，切入一个新领域时通常需要先阅读综述。但现有综述存在三个问题：

| 痛点 | 说明 |
|---|---|
| **综述难找** | 细分方向的综述很少被收录在主流期刊中 |
| **综述过时** | 发表周期长，AI 领域半年就迭代一轮 |
| **太宽泛** | 综述覆盖整个领域，但我的方向只是其中一个小分支 |

手动做综述的流程：检索 → 读 20-30 篇论文 → 记笔记 → 分类对比 → 写作。熟悉的人都知道，这至少需要 1-2 周。

### 1.2 设计目标

- **端到端自动化**：用户只输入一句话，系统输出一篇结构完整的 LaTeX 综述
- **零引用幻觉**：学术写作中捏造引用是红线，必须确保每条引用都可追溯
- **模块化可观测**：知道每一步做了什么，出了错能定位到具体环节

---

## 二、整体架构设计

### 2.1 多 Agent 协作管线

我采用了**五 Agent 线性协作**架构，数据依次流过每个 Agent：
用户输入模糊主题 │ ▼ ┌─────────────────────────────────────────────────────────────┐ │ [Planner] 意图分析 → arXiv 检索策略生成 │ │ 输入: 中文模糊主题 │ │ 输出: 结构化 JSON (含英文检索式 + 备选策略) │ ├─────────────────────────────────────────────────────────────┤ │ [Searcher] arXiv 检索 → Semantic Scholar 引用过滤 → 并发下载│ │ 输入: 检索策略 JSON │ │ 输出: PDF 文件 + 检索元数据 JSON │ ├─────────────────────────────────────────────────────────────┤ │ [Reader] MinerU 解析 PDF → LLM 提取结构化信息 │ │ 输入: PDF 文件 │ │ 输出: 结构化论文信息 JSON │ ├─────────────────────────────────────────────────────────────┤ │ [Writer] 读取 JSON → 构建引用白名单 → LLM 生成 LaTeX │ │ 输入: 论文信息 JSON 列表 │ │ 输出: LaTeX 综述 (.tex) + 白名单 │ ├─────────────────────────────────────────────────────────────┤ │ [Critic] 提取 LaTeX 引用 → 白名单交叉验证 → 输出审计报告 │ │ 输入: LaTeX 文件 + 白名单 │ │ 输出: 审计报告 (合法/幻觉引用清单) │ └─────────────────────────────────────────────────────────────┘ │ ▼ 最终输出: 可编译的 LaTeX 综述 + 零幻觉审计报告


Plain Text


### 2.2 为什么用多 Agent 而不是单 Agent？

这是我在项目迭代中**踩过坑之后**做的决策。

**一开始我尝试了单 Agent 方案**：把检索、阅读、写作、审稿全部塞进一个 Prompt。结果是：
1. 出了问题**不知道卡在哪一步**——是 API 超时？是 PDF 解析失败？还是 LLM 生成幻觉？日志全混在一起
2. Prompt 极其臃肿，很难维护，改一个逻辑可能影响其他地方
3. 资源浪费——如果 PDF 解析失败，前面检索的钱白花了

**拆成多 Agent 之后**：
1. **职责清晰**：每个 Agent 只做一件事，输入输出都是结构化的 JSON 文件
2. **错误隔离**：Reader 挂了不影响 Searcher 已下载的 PDF；Writer 超时不影响 Reader 已解析的 JSON
3. **可观测性**：中间结果全部落盘，打开文件就能看到每一步的数据现场
4. **可重入**：某个 Agent 失败后，可以从上一个 checkpoint 重跑，不用全部重来

**我当时犹豫过的问题**：多 Agent 增加了调用的次数和 token 消耗，值得吗？答案是值得的——在学术写作这个场景下，正确性和可追溯性的优先级远高于成本。

### 2.3 状态管理与容错设计

> **这个问题面试官一定感兴趣——状态管理是区分 Demo 和工程的关键。**

每个 Agent 的输出都会以 JSON / TEX 文件的形式保存到磁盘：
papers_{topic}_{timestamp}/ ← Searcher 输出 ├── *.pdf ← 下载的论文 └── search_metadata.json ← 检索元数据

output_{topic}_{timestamp}/ ← Reader + Writer 输出 ├── mineru_parsed/ ← MinerU 解析结果 ├── {topic}_all_papers.json ← 结构化论文信息 └── {topic}_review.tex ← 生成的 LaTeX 综述


Plain Text


这意味着：如果 Writer 挂了，我只需要修好 Writer 的逻辑，**不需要重新检索和解析**。如果 Critic 发现了幻觉引用，我只需要重新生成 LaTeX，**不需要重新下载和解析 PDF**。

这也带来了一个问题：**当前版本没有做完整的断点续跑**。如果 main.py 跑了一半被中断，下次需要手动指定从哪个步骤继续。这是我知道的不完美的部分，后续可以引入 SQLite 做状态持久化。

---

## 三、各 Agent 详细设计

### 3.1 Planner Agent — 检索策略规划

[planner_agent.py](file:///c:/Users/LOVE/Desktop/write_agent/planner_agent.py)

#### 核心流程
用户输入: "大模型在存储物流中的应用" │ ▼ 调用 LLM (DeepSeek V4 Flash) │ ▼ 结构化 JSON 输出: { "main_query": "all:("Large Language Model" OR "LLM") AND all:("storage logistics" OR "warehouse management")", "alternative_queries": ["ti:LLM AND ti:logistics", ...], "search_rationale": "从核心概念和领域两个维度构建检索式", "suggested_max_papers": 15 }


Plain Text


#### 关键设计决策

| 决策点 | 选择 | 原因 |
|---|---|---|
| arXiv 字段前缀 | 强制要求 `all:` / `ti:` 等前缀 | arXiv API 没有字段前缀会返回 HTTP 500 |
| 输出格式 | 纯 JSON，不要 markdown 代码块 | 减少解析负担，避免正则匹配失败 |
| 温度 | 0.2（较低） | 检索策略需要稳定性，不需要创造性 |
| 失败兜底 | `all:{user_topic}` | 即使 LLM 挂了，也能用原始主题继续 |

#### 迭代改进

**之前的问题**：旧版本的 Planner Prompt 没有强制要求 arXiv 字段前缀，LLM 输出的检索式经常是裸的关键词（如 `"Mixture of Experts"`），导致 arXiv API 返回 500 错误。

**改进**：在 Prompt 中明确要求每个词都必须带字段前缀，并在 fallback 策略中也加了 `all:` 前缀。同时 Searcher 中新增了 `fix_arxiv_query()` 作为二次防御。

---

### 3.2 Searcher Agent — 文献检索与下载

[searcher_agent.py](file:///c:/Users/LOVE/Desktop/write_agent/searcher_agent.py)

#### 核心流程
检索策略 │ ▼ ┌─────────────────────────────────────────────┐ │ 1. arXiv 检索（多搜一倍作为候选池） │ │ search_arxiv(query, max_results * 2) │ │ → 返回 N 篇裸论文（无引用量） │ ├─────────────────────────────────────────────┤ │ 2. Semantic Scholar 引用量查询 │ │ enrich_with_citations(N 篇) │ │ → 每篇论文补上 citation_count 字段 │ │ → 串行查询，间隔 1.5s 避免限流 │ ├─────────────────────────────────────────────┤ │ 3. 引用量排序 + 截取 Top N │ │ filter_and_rank_papers(N 篇, max_papers) │ │ → 按引用量降序 → 取前 max_papers 篇 │ ├─────────────────────────────────────────────┤ │ 4. 线程池并发下载 PDF │ │ batch_download(max_papers, download_dir) │ │ → 最多 3 个并发，避免 arXiv 限流 │ │ → 自动跳过已下载的文件（缓存意识） │ └─────────────────────────────────────────────┘ │ ▼ PDF 文件 + search_metadata.json


Plain Text


#### 关键设计决策

| 决策点 | 选择 | 原因 |
|---|---|---|
| 引用量阈值 | `MIN_CITATION_COUNT = 0` | 不硬性过滤，只做排序参考，避免漏掉低引用但高质量的新论文 |
| 并发下载 | `ThreadPoolExecutor(max_workers=3)` | arXiv 对并发敏感，3 个是安全的平衡点 |
| arXiv 多搜比例 | `max_results * 2` | 候选池比目标多一倍，过滤后有足够的论文可选 |
| 缓存意识 | 检查文件大小 >10KB 则跳过 | 避免重复下载，节省时间和 arXiv 资源 |

#### `fix_arxiv_query()` — 防御性编程示例

```python
def fix_arxiv_query(query: str) -> str:
    """
    自动修正 arXiv 检索式，防止因缺少字段前缀导致 HTTP 500。
    即使 Planner 输出不规范，这里也能兜底。
    """
    valid_prefixes = ['all:', 'ti:', 'au:', 'ab:', 'co:', 'cat:', 'id:']
    has_prefix = any(prefix in query.lower() for prefix in valid_prefixes)
    
    if not has_prefix:
        # 在双引号前加 all:
        fixed_query = re.sub(r'(?
PDF 文件 │ ▼ ┌─────────────────────────────────────────────┐ │ 1. MinerU CLI 解析 │ │ run_mineru(pdf_path, output_dir) │ │ → 调用 subprocess 执行 mineru 命令 │ │ → 输出 Markdown 格式（含公式、表格） │ ├─────────────────────────────────────────────┤ │ 2. 查找 Markdown 文件 │ │ find_md_file(output_dir, pdf_filename) │ │ → 在 MinerU 输出目录中搜索 *.md 文件 │ ├─────────────────────────────────────────────┤ │ 3. LLM 提取结构化信息 │ │ extract_structured_info(md_content) │ │ → 截取前 12000 字符控制 token │ │ → 输出 JSON (title/authors/year/...) │ └─────────────────────────────────────────────┘ │ ▼ 结构化论文信息 JSON


Plain Text


#### 为什么选择 MinerU

MinerU 是由 OpenDataLab 开源的工业级 PDF 解析工具，相比 PyMuPDF 或 pdfplumber：

| 对比项 | PyMuPDF | MinerU |
|---|---|---|
| 双栏排版 | 不支持，文本顺序错乱 | 支持，自动恢复阅读顺序 |
| 公式解析 | 不识别 | 识别为 LaTeX 公式 |
| 表格解析 | 纯文本，结构丢失 | 保留表格结构 |
| 标题层级 | 不识别 | 自动识别并标记 |

学术论文几乎全是双栏排版，PyMuPDF 解析出来的文本段落顺序是乱的（第一栏左下角接第二栏左上角），没法直接使用。

#### 当前已知的不完美之处

1. **长文本截断**：`{markdown_content[:12000]}` 截取了前 12000 字符，如果论文的核心实验在后面的章节，信息会丢失。改进方向是分块阅读 + 多轮提取。
2. **串行处理**：多篇 PDF 是逐篇解析的，没有利用并行。改进方向是使用 asyncio 并发处理。
3. **MinerU 失败无降级**：如果 MinerU 解析失败，不会降级到 PyMuPDF。改进方向是建立多级 fallback。

#### 迭代改进

**旧版本**：硬编码了 API Key 和 Base URL，存在安全风险，换一台机器就得改代码。

**改进**：引入 Pydantic `BaseSettings`，所有配置从 `.env` 文件加载。如果 `.env` 中缺少配置项，程序在启动时就报错（Fail-Fast），而不是在运行到一半才崩溃。同时消除了硬编码，可以安全地提交代码到 GitHub。

---

### 3.4 Writer Agent — 综述撰写

[writer_agent.py](file:///c:/Users/LOVE/Desktop/write_agent/writer_agent.py)

#### 核心流程
结构化论文 JSON 列表 │ ▼ ┌─────────────────────────────────────────────┐ │ 1. 构建引用白名单 │ │ whitelist = {"paper_1": "标题A", ...} │ │ → 只有白名单中的论文才能被引用 │ ├─────────────────────────────────────────────┤ │ 2. 分章节生成 LaTeX │ │ _generate_section("引言", ...) │ │ _generate_section("核心方法对比", ...) │ │ _generate_section("实验与局限性分析", ...) │ │ _generate_section("总结与未来展望", ...) │ │ → 每个章节独立调用 LLM，避免输出截断 │ │ → 若某章节失败，只重试该章节 │ ├─────────────────────────────────────────────┤ │ 3. 后处理校验 (零幻觉防线) │ │ re.sub(r"\cite{([^}]+)}", check, ...) │ │ → 提取所有 \cite{} 标签 │ │ → 不在白名单中的替换为 [引用无效] │ ├─────────────────────────────────────────────┤ │ 4. 拼接：章节 + 参考文献 │ │ full_latex = sections + bib_section │ └─────────────────────────────────────────────┘ │ ▼ LaTeX 综述 (.tex) + 引用白名单


Plain Text


#### 零幻觉的三层防线

这是整个项目最核心的设计，也是面试中会被深挖的部分：

| 防线层级 | 实现方式 | 作用位置 |
|---|---|---|
| **第一层：Prompt 约束** | 在 Prompt 中明确写死引用格式 `\\cite{paper_X}`，声明"违反规则将导致系统崩溃" | 生成时 |
| **第二层：后处理正则校验** | `re.sub()` 提取所有引用标签，逐个与白名单比对，非法引用原地替换 | 生成后 |
| **第三层：Critic 独立审计** | 独立 Agent 重新提取引用并交叉验证，输出审计报告 | 生成后 |

**为什么需要三层而不是一层？** 因为每层都有各自的盲区：
- Prompt 约束可能被模型忽略（模型本质上是概率预测，不是规则引擎）
- 后处理校验只能发现格式不对的引用，发现不了"引用了正确的 paper_id 但在错误的上下文中使用"
- Critic 做的是集合级别的校验，确保没有漏网之鱼

#### 分章节生成 — 解决长文本截断

**旧版本**：一次调用 LLM 生成整篇综述（引言 + 方法 + 实验 + 总结）。如果 LLM 的输出 token 上限是 8K，综述太长就会被截断，LaTeX 不完整。

**改进**：将综述拆为 4 个章节，每个章节独立调用 LLM，最后拼接。好处是：
1. 每个章节的长度可控，不会触发截断
2. 某个章节生成失败，只需要重试那一个章节
3. 可以后续扩展到并发生成（快 4 倍）

**犹豫过的选择**：分章节生成意味着 4 次 LLM 调用，而不是 1 次。Token 消耗增加了，但正确性和完整性的收益远大于这个成本。

#### 跨语言调用封装

Writer Agent 的一个特殊之处是：它使用了 `AsyncOpenAI`（异步客户端），但 `main.py` 是同步调度。解决方案是在同步函数内部用 `asyncio.run()` 包裹异步调用：

```python
def generate_latex_review(...):
    # 这是一个同步函数
    async def _async_llm_call():
        client = AsyncOpenAI(...)
        response = await client.chat.completions.create(...)
        return response
    
    return asyncio.run(_async_llm_call())  # 同步包装异步
```

这样外部调用者不需要感知内部是异步的，接口保持简单。这是 **Adapter 模式**的一种应用。

---

### 3.5 Critic Agent — 零幻觉交叉验证

[critic_agent.py](file:///c:/Users/LOVE/Desktop/write_agent/critic_agent.py)

#### 核心流程
LaTeX 文件 + JSON 白名单 │ ▼ ┌─────────────────────────────────────────────┐ │ 1. 正则提取所有引用标签 │ │ r'\cite{([^}]+)}' → ["paper_1", ...] │ ├─────────────────────────────────────────────┤ │ 2. 集合差集比对 │ │ set(citations) - set(whitelist.keys()) │ │ → 差集中的就是幻觉引用 │ ├─────────────────────────────────────────────┤ │ 3. 输出审计报告 │ │ - 合法引用: N 个 │ │ - 幻觉引用: M 个 (列出具体标签) │ └─────────────────────────────────────────────┘ │ ▼ 零幻觉审计报告


Plain Text


#### 当前已知的不完美之处

Critic 目前只做了**标签级别的集合校验**，也就是检查 `paper_X` 这个 ID 是否在白名单中。它**不做语义级别的校验**——比如一篇论文确实在白名单中，但 LLM 在错误的上下文中引用了它，Critic 发现不了。

改进方向：引入 LLM 做语义级别的引用合理性判断，但代价是额外的 token 消耗和延迟。

---

### 3.6 Main Orchestrator — 总调度器

[main.py](file:///c:/Users/LOVE/Desktop/write_agent/main.py)

调度器的工作是线性串联 5 个 Agent，并在关键节点插入用户确认：
Step 1: 输入研究主题 Step 2: Planner → 展示检索策略 → 用户确认/修改 Step 3: Searcher → 展示候选论文 → 用户确认下载量 → 下载 Step 4: Reader → 用户确认是否解析 → 解析 Step 5: Writer → 输入写作要求 → 生成 LaTeX Step 6: Critic → 自动审计 → 输出报告


Plain Text


关键设计：**用户确认机制**。每个步骤前用户都可以选择跳过或修改，防止全自动流程偏离预期。这是一个 Human-in-the-loop 的实践。

---

## 四、项目迭代历程与关键决策

### 4.1 整体演进
V0 (单 Agent) ├── 一个 Prompt 做所有事情 ├── 问题：定位问题困难，容错差 └── 教训：不要把所有逻辑塞进一个 Prompt │ ▼ V1 (多 Agent 基础版) ├── 拆分为 5 个独立 Agent ├── 硬编码 API Key 和配置 └── 问题：安全风险，改配置要改代码 │ ▼ V2 (配置解耦 + 防御性编程) ├── Pydantic BaseSettings + .env ├── fix_arxiv_query() 自动修正检索式 └── 每个 Agent 独立容错 │ ▼ V3 (零幻觉防线 + 分章节生成) ├── 三层零幻觉机制 ├── 分章节生成避免截断 └── Critic 独立审计


Plain Text


### 4.2 关键决策清单

| 决策 | 选择的方案 | 放弃的方案 | 理由 |
|---|---|---|---|
| **Agent 数量** | 5 个独立 Agent | 1 个全能 Agent | 可观测性、容错性、可维护性 |
| **PDF 解析** | MinerU | PyMuPDF / pdfplumber | MinerU 对学术论文的双栏排版效果更好 |
| **引用排重** | Semantic Scholar 引用量排序 | 只按相关性排序 | 引用量是论文影响力的代理信号 |
| **API 配置** | Pydantic BaseSettings + .env | 硬编码 / JSON 配置 | 类型校验、Fail-Fast、安全 |
| **并发下载** | ThreadPoolExecutor | asyncio + aiohttp | requests 是同步库，ThreadPoolExecutor 更简单 |
| **综述生成** | 分章节 4 次调用 | 单次全篇生成 | 避免输出截断，某章失败只需重试一章 |
| **幻觉校验** | 三层防线（Prompt + 正则 + Critic） | 只用 Prompt 约束 | 每层有盲区，三层互补 |
| **同步/异步** | 同步外部接口 + 内部 async | 纯同步 / 纯异步 | 兼顾 main.py 的简单调用和 LLM 的性能优势 |

---

## 五、与面试考点的对应关系

面试官的问题可以归为 5 个模块，我标明每个模块在这个项目中的体现：

### 模块一：Agent 核心原理与架构设计

| 面试问题 | 项目对应 |
|---|---|
| **ReAct 范式是什么？优缺点？** | 本项目没有显式实现 ReAct 循环，而是采用 Plan-then-Execute 的线性管线。优势是可预测性高、不会死循环；劣势是不够灵活，不能根据中间结果动态调整后续计划 |
| **Function Calling 流程？** | 五个 Agent 本质上就是五个工具函数。main.py 作为调度器判断何时调用哪个工具，传递参数，接收结果 |
| **JSON 不标准怎么处理？** | Planner 中用 `re.sub` 清洗 markdown 标记 + `json.loads` 解析 + try-except 兜底返回默认值。这是"前置约束 + 后置校验 + 参数兜底"三层策略 |
| **Agent 乱调工具/幻觉怎么缓解？** | Writer 的零幻觉三层防线 + Critic 独立审计。核心思路是"不在输入侧做单点防御，在输出侧做多层校验" |
| **多 Agent 协作模式？上下文怎么传递？** | 线性管线模式（Pipeline），上下文通过共享文件系统传递（JSON 落盘），而不是在内存中传引用。这避免了 Token 超限，且天然支持断点续跑 |

### 模块二：RAG 核心技术与长文档场景

| 面试问题 | 项目对应 |
|---|---|
| **怎么优化检索精度？** | 双数据源（arXiv + Semantic Scholar）交叉验证。Query 侧：Planner 做意图分析。检索侧：引用量排序。后处理侧：用户确认机制 |
| **长文档处理？** | PDF 解析后截取前 12000 字符给 LLM。这其实是偷懒的做法，改进方向是分块阅读 + 多轮提取 |

### 模块三：工程化落地与系统稳定性

| 面试问题 | 项目对应 |
|---|---|
| **多用户高并发怎么设计？** | 当前是单用户脚本，没有服务化。这是改进方向之一 |
| **Agent 如何保证安全？** | Human-in-the-loop：每个步骤用户确认。输入校验：`sanitize_filename()` 清理文件路径 |
| **异常处理和降级？** | 每个 Agent 都有 try-except + 兜底返回值。Searcher 有重试机制和缓存意识。MinerU 失败时 Reader 返回 None 而非崩溃 |

### 模块四：业务场景、项目深挖与指标量化

| 面试问题 | 项目对应 |
|---|---|
| **从 0 到 1 的 Agent 项目？如何解决胡言乱语？** | 详见"项目背景与动机"和"零幻觉三层防线" |
| **业务收益量化？** | 30 篇核心文献的综述从 1-2 周缩短到 10 分钟。Critic 审计可以实现引用 100% 可追溯 |
| **如何迭代 Prompt 降低幻觉？** | 通过 Critic 的审计报告收集 Bad Case → 分析是检索没召回还是模型误引 → 修改 Prompt 或补充白名单 |

### 模块五：综合评估体系

| 面试问题 | 项目对应 |
|---|---|
| **如何设计评价指标？** | 组件级：PDF 解析成功率、引用量排序有效性。系统级：端到端耗时（10 分钟）。业务级：幻觉引用数（目标为 0）、用户对综述的满意度 |

---

## 六、项目中不完美的部分（面试中主动展示）

面试官喜欢看到候选人对自己项目的局限性有清醒的认识。以下是我知道的不完美之处：

1. **没有缓存层**：相同主题重复运行会重新调 LLM、重新下载 PDF，浪费 token 和时间。后续可以引入 SQLite 缓存。
2. **检索源单一**：只接了 arXiv，没有知网、PubMed、IEEE。中文论文无法处理。
3. **Reader 长文本截断**：粗暴截取前 12000 字符，后面的信息丢失。应该做分块阅读 + 多轮提取。
4. **没有单元测试**：关键函数没有覆盖率保证。后续可以用 pytest + CI 补齐。
5. **没有 Docker 化**：部署环境需要手动安装 MinerU 和依赖，不够便捷。
6. **Critic 校验太浅**：只做 ID 级别的集合比对，不做语义级别的引用合理性判断。
7. **没有日志系统**：目前用 `print`，线上环境应该用 `logging` 模块分级输出。
8. **主流程是线性的**：如果 PDF 有 30 篇，Reader 串行处理很慢。可以改为并行处理。

---

## 七、面试话术建议

### 7.1 自我介绍版本（30 秒）

> "我设计并实现了一个多 Agent 学术文献综述自动生成系统。用户只需输入一句模糊的中文研究主题，系统会自动完成 arXiv 检索、MinerU PDF 解析、LLM 结构化信息提取、LaTeX 综述撰写和引用零幻觉校验。系统拆分为 Planner、Searcher、Reader、Writer、Critic 五个独立 Agent，通过共享文件系统传递上下文。核心技术挑战是如何在学术写作中保证引用零幻觉，我设计了 Prompt 约束、后处理正则校验、Critic 独立审计三层防线。"

### 7.2 项目深挖版本（3 分钟）

> "这个项目起源于我自己做科研时的痛点：切入一个新领域时要花一两周读论文写综述，而现有的综述要么太宽泛要么已过时。所以我想做一个端到端自动化的系统。
>
> 一开始我尝试了单 Agent 方案，但发现出了问题很难定位。所以拆成了五个 Agent，每个只做一件事，输入输出都是结构化 JSON。
>
> 最大的技术挑战是零幻觉。学术写作中捏造引用是红线，所以我设计了三层防线：第一层在 Prompt 中强制约束引用格式，第二层在后处理中用正则逐个校验引用标签，第三层用 Critic Agent 独立审计。三层互补，每层覆盖前一层可能漏掉的问题。
>
> 另一个迭代是分章节生成。之前一次调用 LLM 生成整篇综述，输出过长会被截断。现在是分四个章节独立生成再拼接，每个章节都很短，永远不会被截断，而且某个章节失败只需要重试那一章。
>
> 这个项目肯定还有改进空间。比如缺少缓存层、检索源单一、没有单元测试。但也正因为知道这些不完美，我清楚后续的优化方向。"

### 7.3 面试官问缺陷时的回答

> "这个项目我一直在迭代，有几个我知道的问题：一是没有缓存层，相同主题重复跑会浪费 token 和下载时间；二是检索源只有 arXiv，不支持中文论文；三是 Reader 的长文本处理比较粗暴，只截取了前 12000 字符；四是目前没有单元测试。不过这些问题都有明确的改进方向——缓存可以用 SQLite，中文检索可以接知网 API，长文本可以用分块阅读，测试可以用 pytest。项目的模块化设计也使得这些改进可以独立进行，不会牵一发动全身。"

---

## 八、项目文件结构
write_agent/ ├── main.py # 主调度器（6 步流程编排） ├── planner_agent.py # Agent 1：检索策略规划 ├── searcher_agent.py # Agent 2：文献检索与下载 ├── reader_agent.py # Agent 3：PDF 深度解析 ├── writer_agent.py # Agent 4：LaTeX 综述撰写 ├── critic_agent.py # Agent 5：零幻觉交叉验证 ├── .env # 配置文件（API Key / Base URL / Model Name） ├── MinerU/ # PDF 解析引擎（外部依赖） ├── project_description.md # 本文档 ├── *copy.py # 旧版本存档（用于对比迭代过程） └── outputs/ # 运行时生成的输出目录 ├── papers_{topic}{ts}/ └── output{topic}_{ts}/


Plain Text


---

> 最后更新：2026 年 6 月
这份文档你可以直接复制保存为 project_description.md。它的特点：

覆盖了你要求的全部 5 个模块 — 从 Agent 原理到 RAG 到工程化到业务量化到评估体系
每个 Agent 都有独立的详细分析 — 流程、代码、决策理由、迭代历史
明确标出了项目中不完美的部分 — 面试官喜欢看到你对自己项目的清醒认知
附带了面试话术 — 30 秒/3 分钟/缺陷回答三种场景
强调了"为什么"而不是"是什么" — 你犹豫过的选择、放弃的方案、选择的原因
这应该是你可以直接用来准备面试的版本。需要调整某个部分的话告诉我就好。